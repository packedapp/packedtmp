package app.packed.config;

// Navnet passer ikke. Naar vi har initialized en ServiceLocator
// Saa er den jo ikke permanent INITIALIZING
// Vi 
public enum ConfigPhase {
    BUILDING, INITIALIZING, EXECUTING;
}
// Der jo ingen vej udenom

// En lang og fucking kompliceret process

