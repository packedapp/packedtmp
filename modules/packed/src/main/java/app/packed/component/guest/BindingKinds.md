General
  HandleConstant          -> A constant per handle
  ComputedHandleConstant  -> A computed constant per handle
  FrameworkProvided       -> Provided by the framework (For example, ManagedLifecycle.stop)


Specific
  OperationInvoker        -> An invoker for the operation    (Might be shared by
  
  
I think take a ComponentHandle.class as parameter when you create the bean