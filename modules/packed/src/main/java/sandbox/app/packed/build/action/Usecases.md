Debugging, how was the application built as it is.
  What where the steps
  
  For example, how was X component installed (Ohh, it was via a container hook)
  

- We create Class proxies for all beans (ProxyClassBeanHook)