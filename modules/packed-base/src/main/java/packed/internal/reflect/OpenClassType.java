package packed.internal.reflect;

import app.packed.container.Extension;

//Ideen er at vi ved hvad vi bruger OpenClass til....

// Taenker paa den maade at scanne nogle ting inde...
public enum OpenClassType {

    /** Used for subclasses of {@link Extension}. */
    EXTENSION;
}
