package app.packed.introspection;

/**
 * An immutable type variable descriptor.
 * 
 * @apiNote In the future, if the Java language permits, {@link TypeVariableDescriptor} may become a {@code sealed}
 *          interface, which would prohibit subclassing except by explicitly permitted types.
 */
// IsBound??? Refe
public interface TypeVariableDescriptor extends VariableDescriptor {

}
