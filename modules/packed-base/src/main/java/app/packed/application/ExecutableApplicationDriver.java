package app.packed.application;

//RunnableAD?
public non-sealed interface ExecutableApplicationDriver<A> extends ApplicationDriver<A> {

    // Man kan ikke seatte launchmode paa en basic bitch
}

// Launchmodes initialized, starting, running, 

// LaunchMode -> Initialize, Starting, Running, Execute